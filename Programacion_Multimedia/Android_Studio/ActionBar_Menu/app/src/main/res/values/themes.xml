<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.ActionBar_Menu" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryVariant">@color/teal_900</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/orange_500</item>
        <item name="colorSecondaryVariant">@color/orange_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:navigationBarColor">@color/teal_700</item>

        <!-- Toolbar / ActionBar -->
        <item name="actionBarStyle">@style/Widget.ActionBar.Custom</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/gray_600</item>
    </style>

    <!-- Theme wrapper -->
    <style name="Theme.ActionBar_Menu" parent="Base.Theme.ActionBar_Menu" />

    <!-- Custom ActionBar -->
    <style name="Widget.ActionBar.Custom" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="background">@color/teal_700</item>
        <item name="titleTextStyle">@style/ActionBar.TitleText</item>
    </style>

    <!-- ActionBar title -->
    <style name="ActionBar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
